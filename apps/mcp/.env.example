# Supabase project (same values the web app uses; the anon key is public).
SUPABASE_URL=https://uavconxsqplwzbhsfgdx.supabase.co
SUPABASE_ANON_KEY=

# The Clarity account the server acts as. RLS scopes everything to this user.
CLARITY_EMAIL=
CLARITY_PASSWORD=

# Optional: act in a specific (e.g. shared) space instead of the personal one.
# CLARITY_SPACE_ID=
